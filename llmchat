#!/bin/bash
# Simple wrapper script for the CLI LLM Chat app

# Get the directory of this script
DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"

# Run the Python script with all arguments passed through
python "$DIR/run.py" "$@"
